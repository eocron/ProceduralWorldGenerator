<ContentControl x:Class="ProceduralWorldGenerator.Views.CreateNodes.CreateNodeBaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Nodify.Shared.Converters;assembly=Nodify.Shared"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <ContentControl.Resources>
        <ResourceDictionary Source="CreateNodeViewResources.xaml"/>
    </ContentControl.Resources>
    <ContentControl.Template>
        <ControlTemplate TargetType="ContentControl">
            <Border Style="{StaticResource BorderStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                    <ContentPresenter/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Grid.Row="0" Command="{Binding OkButtonCommand}" IsDefault="True" IsEnabled="{Binding Path=Content.(Validation.HasError), Converter={converters:InverseBooleanConverter}}">
                            <TextBlock Text="Ok" />
                        </Button>
                        <Button Grid.Column="1" Grid.Row="0" Command="{Binding CancelButtonCommand}" IsCancel="True">
                            <TextBlock Text="Cancel" />
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>
        </ControlTemplate>
    </ContentControl.Template>
    
</ContentControl>
